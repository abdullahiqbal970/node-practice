name: node-app

on:
  push:
    branches:
      - master
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: check-out
        uses: actions/checkout@v4
      - name: build docker images
        run:  docker build -t vue-practice-activity-app:latest .